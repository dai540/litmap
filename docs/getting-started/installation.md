# Installation

Install the package:

```bash
pip install -e .
```

Install the documentation toolchain:

```bash
pip install -e .[docs]
```

Build the Sphinx site:

```bash
sphinx-build -b html docs docs/_build/html
```
